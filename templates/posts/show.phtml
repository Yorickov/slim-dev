{% extends 'layouts/app.phtml' %}

{% block content %}
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-12 col-lg-10 col-xl-8">
        <div class="mb-5 pb-3">
          <h1 class="mt-2 mb-4">{{ post.name }}</h1>
          <div class="my-4">{{ post.body }}</div>
        </div>
      </div>
    </div>
  </div>
{% endblock %}
